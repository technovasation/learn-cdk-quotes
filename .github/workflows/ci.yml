name: CI Example
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      - name: Run a Script
        run: echo "Hello, GitHub Actions!"
